<table class="table table-striped table-dark">
 <thead>
   <tr>
     <th scope="col">Logo</th>
     <th scope="col">Name</th>
     <th scope="col">Open</th>
     <th scope="col">High</th>
     <th scope="col">Low</th>
     <th scope="col">Change %<i class="fa fa-arrow-circle-o-up"></i><i class="fa fa-arrow-circle-o-down"></i></th>
     <th scope="col">Price </th>
     <th scope="col">Tracking</th>
   </tr>
 </thead>
 <tbody>

   <% c_arr = [] %>
   <% c_id = "" %>
   <% @data["DISPLAY"].each do |k, v| %>
       <tr>
         <td><img src="<%= @coins.find_by(name: k).imageurl %>" alt="profile_img" width="30px"></td>
         <td><%= @coins.find_by(name: k).fullname %>
           <%c_id << @coins.find_by(name: k).id %></td>
         <td><%= v["USD"]["OPENDAY"] %></td>
         <td><%= v["USD"]["HIGHDAY"] %></td>
         <td><%= v["USD"]["LOWDAY"] %></td>
         <% if v["USD"]["CHANGEPCTDAY"].to_f < 0 %>
         <td class="price"><%=v["USD"]["CHANGEPCTDAY"] %></td>
         <% else %>
         <td class="price">&nbsp<%=v["USD"]["CHANGEPCTDAY"] %></td>
         <% end %>
         <td><%= v["USD"]["PRICE"] %></td>
         <td><input id="checkBox" type="checkbox"></td>
       </tr>
       <% c_arr << k %>
       <% c_arr << v["USD"]["PRICE"] %>
   <% end %>
     <div class= "hidden" id="user_coins"><%= c_arr %></div>
</tbody>
</table>

<script type="text/javascript">

$('.price').each(function() {
    if (parseFloat($(this).text()) < 0) {
        $(this).addClass('lowprice');
    }
});

(function titleScroller(text) {
    document.title = text;
    setTimeout(function () {
        titleScroller(text.substr(1) + text.substr(0, 1));
    }, 500);
}(user_coins.innerHTML.replace(/[['"\]]+/g, '')));
</script>
